import streamlit as st


def render_header():
    st.title("FinanTec Data Pipeline")
    st.caption(
        "Organização financeira baseada em pipeline ETL, "
        "dashboard e IA generativa."
    )